import React from "react";
import ReactDOM from "react-dom/client";
import FreelanceFinder from "./index.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FreelanceFinder />
  </React.StrictMode>
);
